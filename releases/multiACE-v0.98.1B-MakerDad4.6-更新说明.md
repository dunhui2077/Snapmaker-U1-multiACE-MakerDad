# v0.98.1B-MakerDad4.6

本次为 4.6 修复包，版本号保持不变。

## 修复

- 修复 ACE2 Pro 已在辅助送料时，重复发送 `start_feed_assist` 后返回 `error_2` 被误判为无料失败的问题。
- 打印中工具头切换导致主机辅助送料缓存丢失时，先读取 ACE 实际槽位状态；已处于 `assisting` 时恢复主机缓存，不再重复下发启动指令。
- 回包收到 `error_2` 时再次核验当前槽位；确认仍在辅助送料即视为正常，不重试、不弹出“此通道将不会进料”。

真正未启动辅助送料的通道仍会按照原有重试次数处理，并保留最终无料错误提示。
